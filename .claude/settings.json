{
  "language": "spanish",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // empty' | grep -q 'git commit' && echo '{\"systemMessage\": \"Antes de commitear: ¿necesitan actualizarse README.md o SPEC.md?\"}' || true"
          }
        ]
      }
    ]
  }
}
